# Local database directory

SQLite file `zamili-studio.db` is created automatically when you run `npm run dev` or `npm run setup`.

This folder is gitignored — each developer gets their own local projects database.
